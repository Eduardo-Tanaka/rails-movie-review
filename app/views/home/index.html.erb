<h1>Latest Movies</h1>

<div class="row">
	<% @movies.each do |movie| %>
		<div class="col-md-3">
			<%= link_to image_tag(movie.image_url, size:"280x320", class: "thumbnail"), movie_path(movie) %>
			<p><%= movie.title %></p>
		</div>
	<% end %>
</div>